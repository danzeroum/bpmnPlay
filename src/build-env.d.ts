// Constantes injetadas em tempo de build pelo Vite (`define` em vite.config.ts).
// A versão e o commit do submódulo `bpmn/` — a fronteira "versão pinada e
// visível" (regra nº 2 do Handoff 21) — aparecem no footer.
declare const __BPMN_LIB_VERSION__: string;
declare const __BPMN_LIB_COMMIT__: string;
